services:
  devcontainerissues:
    # set this to the premade image generated by running the 'original_container'
    image: vsc-devcontainer_issues-3acdad642bf0b8a837350e020392641aa40ecb5c170d1273c5af29fb160f012d
    environment:
      FOO: bar
      ROSCONSOLE_FORMAT: "foo"  # this has no effect since it's overwritten by the feature-compose-override file which is corrupted
    command: sleep infinity
